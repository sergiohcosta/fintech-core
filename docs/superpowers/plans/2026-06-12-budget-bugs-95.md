# Budget Planning — Bug Fixes #95 (Bugs 1, 2 e 3)

> **Status: CONCLUÍDO — 2026-06-12**

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir 3 bugs na feature de planejamento mensal: (1) dialog de abertura de ciclo não pedia o dia de início; (2) datepicker da data prevista não abria; (3) status exibido como valor raw do enum em inglês.

**Architecture:** Cada bug é independente. Bug 1 envolve backend + frontend (novo campo em `BudgetCycleOpenRequest`). Bug 2 é um typo de atributo Angular Material no template. Bug 3 é uma tradução de label no componente.

**Tech Stack:** Java 21 / Spring Boot 4, Angular 21 Zoneless, Angular Material 3, OpenAPI spec-first.

---

## Mapa de Arquivos

| Arquivo | Mudança |
|---------|---------|
| `api-spec/openapi.yaml` | Adiciona `startDay` em `BudgetCycleOpenRequest` |
| `backend/src/main/resources/static/openapi.yaml` | Idem (cópia para Swagger UI) |
| `backend/.../dto/budget/BudgetCycleOpenRequest.java` | Adiciona campo `startDay` |
| `backend/.../service/BudgetCycleService.java` | Muda assinatura `open()` para aceitar request; usa `startDay` do request; salva no tenant |
| `backend/.../controller/BudgetCycleController.java` | Passa request completo para `cycleService.open()` |
| `backend/.../service/BudgetCycleServiceTest.java` | Adiciona `@Mock TenantRepository`; atualiza chamadas `open()` para novo request |
| `frontend/.../api/fintechSaaSAPI.schemas.ts` | Adiciona `startDay` em `BudgetCycleOpenRequest` |
| `frontend/.../budget-item-form/budget-item-form.ts` | Adiciona `startDay` ao `cycleForm`; muda close para retornar objeto |
| `frontend/.../budget-item-form/budget-item-form.html` | Adiciona campo dia de início; corrige `matSuffix` → `matIconSuffix` |
| `frontend/.../budget-cycle-current/budget-cycle-current.ts` | Recebe `BudgetCycleOpenRequest` do dialog; adiciona `statusLabel()` |
| `frontend/.../budget-cycle-current/budget-cycle-current.html` | Usa `statusLabel(item.status)` nos 3 chips de status |

---

## Task 1: OpenAPI spec — adiciona `startDay` em `BudgetCycleOpenRequest`

**Files:**
- Modify: `api-spec/openapi.yaml` (linha ~641)
- Modify: `backend/src/main/resources/static/openapi.yaml` (linha ~641)

- [ ] **Step 1: Atualizar `api-spec/openapi.yaml`**

Substituir o bloco `BudgetCycleOpenRequest` (linhas ~641-648):

```yaml
    BudgetCycleOpenRequest:
      type: object
      required: [referenceMonth, startDay]
      properties:
        referenceMonth:
          type: string
          pattern: '^\d{4}-\d{2}$'
          example: "2026-06"
        startDay:
          type: integer
          minimum: 1
          maximum: 28
          example: 1
          description: "Dia do mês em que o ciclo começa (1 = mês calendário)"
```

- [ ] **Step 2: Replicar a mesma mudança em `backend/src/main/resources/static/openapi.yaml`**

Localizar o bloco `BudgetCycleOpenRequest` e aplicar a mesma substituição acima.

- [ ] **Step 3: Commit**

```bash
git add api-spec/openapi.yaml backend/src/main/resources/static/openapi.yaml
git commit -m "chore(spec): adiciona startDay ao BudgetCycleOpenRequest"
```

---

## Task 2: Backend — DTO, Service e Controller

**Files:**
- Modify: `backend/src/main/java/com/fintech/api/dto/budget/BudgetCycleOpenRequest.java`
- Modify: `backend/src/main/java/com/fintech/api/service/BudgetCycleService.java`
- Modify: `backend/src/main/java/com/fintech/api/controller/BudgetCycleController.java`

- [ ] **Step 1: Atualizar `BudgetCycleOpenRequest.java`**

```java
package com.fintech.api.dto.budget;

import jakarta.validation.constraints.*;

public record BudgetCycleOpenRequest(
    @NotBlank @Pattern(regexp = "\\d{4}-\\d{2}", message = "Formato esperado: yyyy-MM")
    String referenceMonth,

    @NotNull @Min(1) @Max(28)
    Integer startDay
) {}
```

- [ ] **Step 2: Atualizar `BudgetCycleService.open()` para usar o request completo**

Alterar a assinatura e a lógica em `BudgetCycleService.java`:

```java
@Transactional
public BudgetCycle open(Tenant tenant, User user, BudgetCycleOpenRequest req) {
    Tenant managed = tenantRepository.findById(tenant.getId())
        .orElseThrow(() -> new EntityNotFoundException("Tenant não encontrado."));

    if (cycleRepository.findByTenantAndStatus(managed, BudgetCycleStatus.OPEN).isPresent()) {
        throw new IllegalStateException("Já existe um ciclo aberto para este tenant.");
    }

    int startDay = req.startDay();
    LocalDate[] dates = calculateCycleDates(YearMonth.parse(req.referenceMonth()), startDay);
    LocalDate startDate = dates[0];
    LocalDate endDate   = dates[1];

    if (cycleRepository.existsOverlap(managed, startDate, endDate)) {
        throw new IllegalStateException("O período solicitado conflita com um ciclo já existente.");
    }

    // Persiste a preferência de dia de início no tenant para próximos ciclos
    managed.setBudgetCycleStartDay(startDay);
    tenantRepository.save(managed);

    BigDecimal opening = accountRepository.sumLiquidBalanceByTenant(
        managed.getId(), TransactionType.INCOME, TransactionStatus.CANCELLED);

    BudgetCycle cycle = cycleRepository.save(BudgetCycle.builder()
        .tenant(managed)
        .startDate(startDate)
        .endDate(endDate)
        .openingBalance(opening)
        .status(BudgetCycleStatus.OPEN)
        .createdBy(user)
        .build());

    populateRecurringItems(cycle, managed, user, startDate, startDay);
    populateInstallmentItems(cycle, managed, startDate, endDate);

    log.info("Ciclo de planejamento aberto [cycleId={} tenantId={} periodo={}/{} startDay={}]",
        cycle.getId(), managed.getId(), startDate, endDate, startDay);
    return cycle;
}
```

- [ ] **Step 3: Atualizar `BudgetCycleController.open()`**

O controller já recebe `BudgetCycleOpenRequest req` via `@RequestBody`. Apenas mudar a chamada ao service:

```java
@PostMapping
public ResponseEntity<BudgetCycleResponseDTO> open(@Valid @RequestBody BudgetCycleOpenRequest req) {
    User user = getUser();
    var cycle = cycleService.open(user.getTenant(), user, req);
    return ResponseEntity.status(201)
        .body(BudgetCycleResponseDTO.fromEntity(cycle, cycleService.listItems(cycle)));
}
```

- [ ] **Step 4: Compilar o backend para verificar erros**

```bash
cd /home/sergio/fintech-core/backend && ./mvnw compile -q
```

Esperado: `BUILD SUCCESS` (os testes vão quebrar ainda — isso é esperado; corrigimos na Task 3).

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/java/com/fintech/api/dto/budget/BudgetCycleOpenRequest.java \
        backend/src/main/java/com/fintech/api/service/BudgetCycleService.java \
        backend/src/main/java/com/fintech/api/controller/BudgetCycleController.java
git commit -m "feat(budget): adiciona startDay ao abrir ciclo e persiste no tenant"
```

---

## Task 3: Testes do backend — corrigir e rodar

**Files:**
- Modify: `backend/src/test/java/com/fintech/api/service/BudgetCycleServiceTest.java`

**Contexto:** Os testes de `open()` estavam com dois problemas pré-existentes:
1. `TenantRepository` não estava mockado — causaria NPE ao chamar `tenantRepository.findById()`
2. `open()` recebia `String referenceMonth` mas agora recebe `BudgetCycleOpenRequest`

- [ ] **Step 1: Adicionar `@Mock TenantRepository` e atualizar imports**

Adicionar na seção de `@Mock` (logo abaixo de `@Mock TransactionRepository transactionRepository`):

```java
@Mock TenantRepository tenantRepository;
```

Adicionar o import:
```java
import com.fintech.api.repository.TenantRepository;
import com.fintech.api.dto.budget.BudgetCycleOpenRequest;
```

- [ ] **Step 2: Atualizar `open_jáExisteCicloAberto_lançaException()`**

```java
@Test
@DisplayName("open() lança IllegalStateException se já existe ciclo OPEN")
void open_jáExisteCicloAberto_lançaException() {
    Tenant tenant = tenantWith(1);
    User user = new User();

    when(tenantRepository.findById(tenant.getId())).thenReturn(Optional.of(tenant));
    when(cycleRepository.findByTenantAndStatus(tenant, BudgetCycleStatus.OPEN))
        .thenReturn(Optional.of(new BudgetCycle()));

    assertThatThrownBy(() -> service.open(tenant, user, new BudgetCycleOpenRequest("2026-06", 1)))
        .isInstanceOf(IllegalStateException.class)
        .hasMessageContaining("ciclo aberto");
}
```

- [ ] **Step 3: Atualizar `open_periodoSobrepoe_lançaException()`**

```java
@Test
@DisplayName("open() lança IllegalStateException se período sobrepõe ciclo existente")
void open_periodoSobrepoe_lançaException() {
    Tenant tenant = tenantWith(1);
    User user = new User();

    when(tenantRepository.findById(tenant.getId())).thenReturn(Optional.of(tenant));
    when(cycleRepository.findByTenantAndStatus(tenant, BudgetCycleStatus.OPEN))
        .thenReturn(Optional.empty());
    when(cycleRepository.existsOverlap(eq(tenant), any(), any()))
        .thenReturn(true);

    assertThatThrownBy(() -> service.open(tenant, user, new BudgetCycleOpenRequest("2026-06", 1)))
        .isInstanceOf(IllegalStateException.class)
        .hasMessageContaining("conflita");
}
```

- [ ] **Step 4: Atualizar `open_calculaOpeningBalance()`**

```java
@Test
@DisplayName("open() calcula openingBalance a partir das contas líquidas do tenant")
void open_calculaOpeningBalance() {
    Tenant tenant = tenantWith(1);
    User user = new User();

    when(tenantRepository.findById(tenant.getId())).thenReturn(Optional.of(tenant));
    when(cycleRepository.findByTenantAndStatus(tenant, BudgetCycleStatus.OPEN))
        .thenReturn(Optional.empty());
    when(cycleRepository.existsOverlap(any(), any(), any()))
        .thenReturn(false);
    when(tenantRepository.save(any())).thenAnswer(inv -> inv.getArgument(0));
    when(accountRepository.sumLiquidBalanceByTenant(
            eq(tenant.getId()), eq(TransactionType.INCOME), eq(TransactionStatus.CANCELLED)))
        .thenReturn(new BigDecimal("3200.00"));
    when(recurringRepository.findAllByTenantAndActiveTrueOrderByDayOfMonthAscDescriptionAsc(tenant))
        .thenReturn(List.of());
    when(transactionRepository.findInstallmentsInPeriodByTenant(any(), any(), any(), any()))
        .thenReturn(List.of());

    var captor = ArgumentCaptor.forClass(BudgetCycle.class);
    when(cycleRepository.save(captor.capture())).thenAnswer(inv -> inv.getArgument(0));

    service.open(tenant, user, new BudgetCycleOpenRequest("2026-06", 1));

    assertThat(captor.getValue().getOpeningBalance()).isEqualByComparingTo("3200.00");
}
```

- [ ] **Step 5: Rodar os testes do service**

```bash
cd /home/sergio/fintech-core/backend && ./mvnw test -pl . -Dtest=BudgetCycleServiceTest -q
```

Esperado: todos os testes passam (BUILD SUCCESS).

- [ ] **Step 6: Commit**

```bash
git add backend/src/test/java/com/fintech/api/service/BudgetCycleServiceTest.java
git commit -m "test(budget): corrige mocks e assinaturas após mudança em BudgetCycleService.open()"
```

---

## Task 4: Frontend — schema gerado + formulário de abertura de ciclo (Bug 1 + Bug 2)

**Files:**
- Modify: `frontend/src/app/core/api/fintechSaaSAPI.schemas.ts` (linha ~401)
- Modify: `frontend/src/app/features/planning/budget-item-form/budget-item-form.ts`
- Modify: `frontend/src/app/features/planning/budget-item-form/budget-item-form.html`
- Modify: `frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.ts`

- [ ] **Step 1: Atualizar `BudgetCycleOpenRequest` no schema gerado**

Em `fintechSaaSAPI.schemas.ts`, substituir:

```typescript
export interface BudgetCycleOpenRequest {
  /** @pattern ^\d{4}-\d{2}$ */
  referenceMonth: string;
}
```

Por:

```typescript
export interface BudgetCycleOpenRequest {
  /** @pattern ^\d{4}-\d{2}$ */
  referenceMonth: string;
  /**
   * @minimum 1
   * @maximum 28
   */
  startDay: number;
}
```

- [ ] **Step 2: Atualizar `budget-item-form.ts`**

Adicionar `startDay` ao `cycleForm` e mudar `onSubmit()` para retornar o objeto completo:

```typescript
readonly cycleForm = this.fb.group({
  referenceMonth: ['', [Validators.required, Validators.pattern(/^\d{4}-\d{2}$/)]],
  startDay: [1, [Validators.required, Validators.min(1), Validators.max(28)]],
});
```

Atualizar `onSubmit()` — bloco do modo `openCycle`:

```typescript
if (this.isOpenCycleMode()) {
  if (this.cycleForm.invalid) return;
  const v = this.cycleForm.getRawValue();
  this.dialogRef.close({ referenceMonth: v.referenceMonth!, startDay: v.startDay! });
}
```

Adicionar import de `BudgetCycleOpenRequest` ao topo:
```typescript
import { BudgetCycleOpenRequest, BudgetItemCreateRequest } from '../../../core/api/fintechSaaSAPI.schemas';
```

Atualizar o tipo de retorno exportado do modo openCycle — não é necessário novo tipo, pois `dialogRef.close()` aceita `any`. Mas o caller vai tipá-lo como `BudgetCycleOpenRequest`.

- [ ] **Step 3: Atualizar `budget-item-form.html`** (Bug 1 + Bug 2 juntos)

Substituir o bloco `@if (isOpenCycleMode())` inteiro:

```html
@if (isOpenCycleMode()) {
  <h2 mat-dialog-title>Abrir ciclo de planejamento</h2>
  <mat-dialog-content>
    <form [formGroup]="cycleForm" style="display:flex;flex-direction:column;gap:12px;padding-top:8px">
      <mat-form-field appearance="outline" style="width:100%">
        <mat-label>Mês de referência (yyyy-MM)</mat-label>
        <input matInput formControlName="referenceMonth" placeholder="2026-06" />
        <mat-error>Formato esperado: yyyy-MM (ex: 2026-06)</mat-error>
      </mat-form-field>

      <mat-form-field appearance="outline" style="width:100%">
        <mat-label>Dia de início do ciclo (1–28)</mat-label>
        <input matInput type="number" formControlName="startDay" min="1" max="28" />
        <mat-hint>Dia 1 = mês calendário; dia 11 = ciclo do dia 11 ao dia 10</mat-hint>
        <mat-error>Informe um dia entre 1 e 28</mat-error>
      </mat-form-field>
    </form>
  </mat-dialog-content>
  <mat-dialog-actions align="end">
    <button mat-button type="button" (click)="onCancel()">Cancelar</button>
    <button mat-flat-button color="primary" type="button"
            [disabled]="cycleForm.invalid" (click)="onSubmit()">Abrir</button>
  </mat-dialog-actions>
}
```

No bloco `@else` (formulário de item), corrigir Bug 2 — trocar `matSuffix` por `matIconSuffix` no toggle do datepicker:

```html
<mat-datepicker-toggle matIconSuffix [for]="picker" />
```

- [ ] **Step 4: Atualizar `budget-cycle-current.ts` — receber `BudgetCycleOpenRequest` do dialog**

Adicionar import de `BudgetCycleOpenRequest` (se ainda não importado):
```typescript
import { BudgetCycleResponse, BudgetItemResponse, BudgetCycleOpenRequest } from '../../../core/api/fintechSaaSAPI.schemas';
```

Substituir o método `openCycle()`:

```typescript
openCycle(): void {
  const ref = this.dialog.open(BudgetItemFormComponent, {
    width: '400px',
    data: { mode: 'openCycle' },
  });
  ref.afterClosed().subscribe((req?: BudgetCycleOpenRequest) => {
    if (!req) return;
    this.planningService.openCycle(req).subscribe({
      next: c => {
        this.cycle.set(c);
        this.items.set(c.items ?? []);
        this.snackBar.open('Ciclo aberto com sucesso.', 'OK', { duration: 3000 });
      },
      error: () => this.snackBar.open('Erro ao abrir ciclo.', 'OK', { duration: 3000 }),
    });
  });
}
```

- [ ] **Step 5: Verificar compilação TypeScript**

```bash
cd /home/sergio/fintech-core/frontend && npx tsc --noEmit 2>&1 | head -30
```

Esperado: sem erros de tipos.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/app/core/api/fintechSaaSAPI.schemas.ts \
        frontend/src/app/features/planning/budget-item-form/budget-item-form.ts \
        frontend/src/app/features/planning/budget-item-form/budget-item-form.html \
        frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.ts
git commit -m "fix(budget): adiciona campo dia de início ao abrir ciclo e corrige datepicker"
```

---

## Task 5: Frontend — tradução dos status (Bug 3)

**Files:**
- Modify: `frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.ts`
- Modify: `frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.html`

- [ ] **Step 1: Adicionar `statusLabel()` ao componente**

Em `budget-cycle-current.ts`, adicionar o método dentro da classe (após `deleteItem()`):

```typescript
statusLabel(status: string | undefined): string {
  const labels: Record<string, string> = {
    PENDING: 'Pendente',
    REALIZED: 'Realizado',
    SKIPPED: 'Ignorado',
  };
  return labels[status ?? ''] ?? (status ?? '');
}
```

- [ ] **Step 2: Atualizar os 3 chips de status no template**

Em `budget-cycle-current.html`, substituir todas as ocorrências de:

```html
<mat-chip>{{ item.status }}</mat-chip>
```

Por:

```html
<mat-chip [class.chip-realized]="item.status === 'REALIZED'"
          [class.chip-skipped]="item.status === 'SKIPPED'">
  {{ statusLabel(item.status) }}
</mat-chip>
```

São 3 colunas `status` nas 3 tabelas (receitas, despesas, parcelas). Substituir as 3.

- [ ] **Step 3: Adicionar estilos para os chips em `budget-cycle-current.scss`**

```scss
.chip-realized {
  background-color: var(--mat-sys-tertiary-container) !important;
  color: var(--mat-sys-on-tertiary-container) !important;
}

.chip-skipped {
  opacity: 0.5;
}
```

- [ ] **Step 4: Verificar compilação TypeScript**

```bash
cd /home/sergio/fintech-core/frontend && npx tsc --noEmit 2>&1 | head -20
```

Esperado: sem erros.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.ts \
        frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.html \
        frontend/src/app/features/planning/budget-cycle-current/budget-cycle-current.scss
git commit -m "fix(budget): traduz status dos itens para português"
```

---

## Self-Review

**Spec coverage:**
- Bug 1 (dia de início no ciclo): Tasks 1, 2, 3, 4 — coberto ✓
- Bug 2 (datepicker não abre): Task 4, Step 3 — coberto ✓
- Bug 3 (status em inglês): Task 5 — coberto ✓

**Verificações de consistência:**
- `BudgetCycleOpenRequest` com `startDay: number` definido na Task 4 Step 1; usado na Task 4 Steps 2, 3, 4 — consistente ✓
- `service.open()` com nova assinatura definida na Task 2 Step 2; controller atualizado na Task 2 Step 3; testes na Task 3 — consistente ✓
- `statusLabel()` definido na Task 5 Step 1; usado no template na Task 5 Step 2 — consistente ✓
- `tenantRepository.save()` adicionado ao mock na Task 3 Step 4 — sem NPE ✓
