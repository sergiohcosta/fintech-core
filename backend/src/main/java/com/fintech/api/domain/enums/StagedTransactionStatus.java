package com.fintech.api.domain.enums;

/** Estado de uma transação em staging: PENDING (aguarda decisão), CONFIRMED (promovida), DISCARDED. */
public enum StagedTransactionStatus { PENDING, CONFIRMED, DISCARDED }
