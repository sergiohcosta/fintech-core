# Project Structure

Layout derivável via `ls`/`find` a partir da raiz — não replicado aqui. Pastas de topo: `api-spec/` (OpenAPI), `backend/` (Java 21 + Spring Boot), `frontend/` (Angular 21 Zoneless), `android/` (Kotlin + Jetpack Compose), `scripts/`, `docs/`.
